# Shared `lib/core` rules index

## Purpose
Maps sections of the app core (barrel, foundation, network, DI, etc.) to enforceable rules. Agents must follow **this app’s** `ai_docs/architecture.md` when folder names differ (e.g. `base/` vs `foundation/`).

## Fill when
- When `lib/core` layout or cross-cutting boundaries change in reference apps.

## References
- Optional paths in **your app repos** (not copied here): e.g. `lib/core/core.dart`, `ai_docs/architecture.md`

## Content
<!-- Fill in later. Leave empty if unknown. -->
