# Firebase and public repositories

## Purpose
Safety rules for `firebase_options.dart`, API keys in repos, and what must never be committed or mirrored publicly.

## Fill when
- When Firebase project layout or secret handling changes.

## References
- Optional paths in **your app repos** (not copied here): e.g. `lib/firebase_options.dart`

## Content
<!-- Fill in later. Leave empty if unknown. -->
