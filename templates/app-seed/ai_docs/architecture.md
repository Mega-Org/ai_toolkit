# App-specific architecture (stub)

Describe this app’s core vs feature boundaries, folder layout under `lib/`, and any exceptions to toolkit defaults.

Fill when the product structure is clear. Prefer linking to code paths over copying toolkit rules.
