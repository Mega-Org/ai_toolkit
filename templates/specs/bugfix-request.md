# Bugfix request

Status: open | in-progress | done
Started: YYYY-MM-DD
External reference:

## Initial command

(Verbatim first user message — e.g. `bugfix: …`)

## Requirements

- (Append explicit needs as the user states them in chat)
- Example: client flavor only
- Example: minimal diff, no refactor
- Example: verify Arabic and English

## Decisions

- (User-approved choices — e.g. fix in cubit, use AppRouter.pop)

## Out of scope (user stated)

- …

## Session notes

(Short chronological notes — not a full chat transcript)
