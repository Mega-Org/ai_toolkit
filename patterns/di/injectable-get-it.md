# injectable and get_it

## Purpose
Implementation patterns for `get_it` + `injectable` that stay aligned with `rules/core/di.md`.

## Fill when
- When module layout or lazy singleton conventions change.

## References
- Optional paths in **your app repos** (not copied here): e.g. `lib/core/di/`

## Content
<!-- Fill in later. Leave empty if unknown. -->
